/= render 'filter'
.inner_content
  .bikes_header
    h1 Покупка и продажа б/у велосипедов
    = link_to 'Продать велосипед', new_profile_bike_path, class: 'sell_bike_btn'
.bikes
  - @bikes.each do |bike|
    a.home_bike href="#{bike_path(bike)}" style="background-image: url(#{bike.pictures.any? ? bike.pictures.first.img.url(:medium) : false})"
      .price.price_hl = human_price bike.price